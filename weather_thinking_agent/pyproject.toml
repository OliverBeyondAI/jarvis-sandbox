[project]
name = "weather-thinking-agent"
version = "0.1.0"
description = "Weather agent using Claude Opus 4.7 extended thinking with MCP tool use"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.9.0",
    "anthropic>=0.97.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
